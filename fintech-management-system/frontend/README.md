# Frontend

React + TypeScript scaffold for FinTech Management System.

## Run
```bash
npm install
npm run dev
```

## Notes
- Uses Axios with JWT Bearer token interceptor.
- Connects to backend base URL via `VITE_API_BASE_URL`.
